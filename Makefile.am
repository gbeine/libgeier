SUBDIRS = src include etc config doc tests

pkgconfigdir = $(libdir)/pkgconfig/
pkgconfig_DATA = \
        libgeier.pc

man_MANS=geier.1

EXTRA_DIST = \
	libgeier.pc.in \
	${man_MANS} 

CLEANFILES = *~

